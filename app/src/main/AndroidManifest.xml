<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.laptop88.ezgo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".view.EzgoApplication"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".view.activity.main.MainScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.booking1.FindTrainsActivity"></activity>
        <!--&lt;!&ndash;<activity android:name=".view.fragment.Steamer.ShowListSteamersDiagram.SteamerFragment">&ndash;&gt;-->

        <!--</activity>-->
        <activity android:name=".view.activity.ShowListTrain.ShowListTrainActivity"></activity>
        <activity android:name=".view.activity.booking.BookingActivity"></activity>
    </application>

</manifest>